{% extends 'base.html.twig' %}

{% block title %}Create Wallet{% endblock %}

{% block body %}
    <h1>Create Wallet</h1>

    {{ form_start(form) }}
    {{ form_widget(form) }}
    <button class="btn btn-primary">Create</button>
    {{ form_end(form) }}

    <a href="{{ path('wallet_index') }}" class="btn btn-secondary">Back to list</a>
{% endblock %}
